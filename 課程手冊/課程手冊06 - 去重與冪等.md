# 第 6 章：讓重跑不再產生重複 — 去重與冪等（upsert）

> 上一章你發現：重跑 producer，資料就翻倍。這一章用一個資料工程的核心觀念「冪等」把它解決掉。

---

## 做完這一章，你會做到

1. 說得出「冪等（idempotent）」是什麼，並知道為什麼定期抓取一定要冪等。
2. 看懂怎麼用「複合主鍵 + `on_duplicate_key_update`（upsert）」做到去重。
3. 親手驗證：這一版無論重跑幾次，資料庫筆數都不變。

---

## 先搞懂：什麼是「冪等」

**打個比方**：電梯的「5 樓」按鈕，你按一次是去 5 樓，連按五次還是去 5 樓——**多按沒有副作用**。這就是冪等：同一個操作做幾次，結果都一樣。

上一章的寫法**不冪等**：每跑一次就 `append` 一批，跑三次就三份重複。但真實情況每天都會重抓、失敗會重試、補資料會重跑，**不冪等的 pipeline 會越跑越髒**。

解法：讓「同一筆資料」有一個唯一身分（主鍵），寫入時如果這筆已經存在就**更新**、不存在才**新增**。這個「有就更新、沒有就新增」的動作叫 **upsert**（update + insert）。

---

## 這一章會用到的檔案

| 檔案 | 角色 | 說明 |
|------|------|------|
| `crawler/tasks_crawler_finmind_duplicate.py` | 任務定義 | `crawler_finmind_duplicate` + `upload_data_to_mysql_duplicate` |
| `crawler/producer_crawler_finmind_duplicate.py` | 生產者 | 派送去重版任務（一樣走多佇列分流）|

---

## 先補個底：DataFrame 與資料清理（線下練熟 → 線上實戰）

等下的 upsert 程式碼會「逐列跑過一個 DataFrame」，先把這個一路用到現在的東西講清楚。

**什麼是 DataFrame**：

- DataFrame 是 Python 套件 pandas 的核心資料結構，可以想像成「**強化版的 Excel 表格**」
- 由**列（rows）**和**欄（columns）**組成，支援不同型態的資料（數字、文字、時間…）
- 專為資料處理設計，能幫你：讀取各種資料來源、檢查與清理資料品質、篩選/轉換/聚合、輸出結果（`to_sql`、`to_csv`——第 5 章做的事）

**空值處理的心法**：

- **爬蟲時就做好防呆、預設值處理**——API 回來的資料可能缺欄位、缺值，越早處理越省事
- 資料還是 list of dict 時要**逐筆**處理，比較沒效率
- 轉成 DataFrame 就能**統一處理**：查看/處理單一欄位、新增/刪除欄位、篩選查詢

### 線下：先用 Jupyter 把 DataFrame 玩熟

repo 準備好了練習本 `example/pandas.ipynb`，一行啟動（不用安裝任何東西，跑完即棄）：

```bash
uv run --with jupyter jupyter lab example/pandas.ipynb
```

Notebook 的內容地圖（用「故意留了空值」的學生資料，每招都看得到效果）：

| 段落 | 練什麼 |
|------|--------|
| 基本資訊 | `head()` / `shape` / `dtypes` / `info()` / `describe()` / `isna().sum()` |
| 篩選資料 | 單欄、多欄、條件篩選 `df[df["score"] > 80]`、`iloc` |
| 資料轉換 | 新增欄位、`astype` 改型別（含允許缺值的 `Int64`）|
| 空值處理 | 找缺值的列、`fillna(0)` / 填平均值 / 填固定日期、`dropna()` |
| 檔案輸出 | `to_csv` / `read_csv` |
| 練習 | 訂單資料 4 題（檢查 → 補值 → 篩選排序 → 新增欄位運算）|
| **真實股價段** | 下面「線上」的同一套流程，在 notebook 裡自己跑一次 |

### 線上：同一套動作，用在真的股價資料（VM 實測輸出）

```python
import requests, pandas as pd

# ① 跟爬蟲任務同一套：打 FinMind API，拿回 list of dict
r = requests.get("https://api.finmindtrade.com/api/v4/data", params={
    "dataset": "TaiwanStockPrice", "data_id": "2330",
    "start_date": "2025-06-01", "token": ""})
data = r.json()["data"]
# 拿到 271 筆，第一筆長這樣：
# {'date': '2025-06-02', 'stock_id': '2330', 'Trading_Volume': 40608468, ..., 'close': 946.0, 'spread': -21.0}
```

```python
df = pd.DataFrame(data)      # ② 一行：list of dict → 表格
df.head(3)
#          date stock_id  Trading_Volume  ...   open    max    min  close  spread
# 0  2025-06-02     2330        40608468  ...  958.0  961.0  946.0  946.0   -21.0
# 1  2025-06-03     2330        27482916  ...  960.0  965.0  950.0  950.0     4.0

df.dtypes
# date      object   ← 注意：日期是字串！入庫時的型別問題（第 5 章 dtype 講過的「用猜的」）
# close    float64 ...
```

```python
df.isna().sum()              # ③ 檢查空值——每一欄都是 0
# 這批資料剛好乾淨；但「檢查」永遠是第一步，真的有洞就用 notebook 練過的 fillna / dropna
```

```python
df["spread_pct"] = (df["spread"] / df["open"] * 100).round(2)  # ④ 新增欄位：漲跌幅 %
df = df.drop(columns=["Trading_turnover"])                     #    刪掉用不到的欄
hot = df[df["Trading_Volume"] > 30_000_000]                    #    篩選：成交量 > 3 千萬股
# 實測：186 / 271 天；例如 2025-06-04 close=990.0、spread_pct=+4.11
```

### 接回主線

這個抓下來、檢查過、加工完的 `df`，就是等一下 `upload_data_to_mysql_duplicate(df)` **逐列 upsert 的那個 df**。整條線是：**拿資料（第 2 章）→ 清資料（本節）→ 冪等入庫（本章主戲）**——現在進主戲。

---

## 一行一行讀懂「去重寫入」

### 第一步：定義資料表結構，指定複合主鍵

```python
from sqlalchemy import BigInteger, Column, Date, Float, MetaData, String, Table

metadata = MetaData()
stock_price_table = Table(
    "TaiwanStockPrice_duplicate",                      # 資料表名稱
    metadata,
    Column("stock_id", String(50), primary_key=True), # 主鍵之一
    Column("date", Date, primary_key=True),           # 主鍵之二
    Column("Trading_Volume", BigInteger),
    Column("open", Float),
    Column("max", Float),
    Column("min", Float),
    Column("close", Float),
    # ...其餘欄位
)
metadata.create_all(engine)   # 表不存在才建立
```

**最關鍵的一句**：`stock_id` 和 `date` 兩個欄位都設了 `primary_key=True`，這叫**複合主鍵**。意思是「同一支股票、同一天」在這張表裡只能有一筆——這就是每筆資料的「唯一身分」。上一章的表沒有主鍵，所以資料庫根本不知道哪兩筆算「重複」；這一章明確告訴它了。

### 第二步：upsert——重複就更新，不重複就新增

```python
from sqlalchemy.dialects.mysql import insert   # MySQL 專用的 insert，支援 on_duplicate_key_update

for _, row in df.iterrows():
    # ① 準備插入這一列
    insert_stmt = insert(stock_price_table).values(**row.to_dict())

    # ② 如果主鍵撞到已存在的資料，就改成「更新所有欄位為新值」
    update_stmt = insert_stmt.on_duplicate_key_update(
        **{col.name: insert_stmt.inserted[col.name]
           for col in stock_price_table.columns}
    )

    # ③ 執行
    with engine.begin() as conn:
        conn.execute(update_stmt)
```

一段一段看：

- **① `insert(...).values(...)`**：準備把這一列（`row`）插入表裡。
- **② `.on_duplicate_key_update(...)`**：這是**MySQL 專用語法**，也是整章的靈魂。它的意思是——「如果這筆的主鍵（stock_id + date）跟已存在的撞到了，別報錯、別製造重複，改成把那些欄位更新成新值」。那個 dict comprehension 就是在說「所有欄位都更新成新值」。
- **③ `with engine.begin() as conn`**：在一個交易（transaction）裡執行，確保寫入的完整性。

所以無論這支任務跑幾次，`TaiwanStockPrice_duplicate` 裡「同股同日」永遠只有一筆，而且是最新的值。**這就是冪等。**

> 對照上一章：那邊用 `to_sql(if_exists="append")`，沒有主鍵概念，只會一直往後疊；這一章用明確的複合主鍵 + upsert，才真正做到冪等。

---

## 一步一步跟著做

### Step 1：基礎設施 + worker（跟前面一樣）

```bash
docker compose -f docker-compose-local.yml up -d rabbitmq flower mysql phpmyadmin
uv run python -m celery -A crawler.worker worker --loglevel=info
```

> 這一版的任務走多佇列（twse / tpex），所以你也可以改開 twse / tpex 兩個 worker（第 3 章的做法）。用單一 worker 也能跑，只是任務會在對應佇列等。這裡先用第 3 章的兩個 worker 最順：
>
> ```bash
> uv run python -m celery -A crawler.worker worker -l info --hostname=twse@%h -Q twse
> uv run python -m celery -A crawler.worker worker -l info --hostname=tpex@%h -Q tpex
> ```

### Step 2：派送去重版任務

```bash
uv run crawler/producer_crawler_finmind_duplicate.py
```

### Step 3：記下筆數（這是驗證冪等的基準）

到 phpMyAdmin（http://localhost:8080，root / 1234）看 `mydb` 裡的 `TaiwanStockPrice_duplicate` 表，記下總筆數。

### Step 4：關鍵實驗——重跑，看筆數變不變

**再跑一次、甚至三次** `producer_crawler_finmind_duplicate.py`，每跑完回去看一次筆數。

> ✅ **筆數應該完全不變。** 這就是冪等的證據。對照第 5 章的 `TaiwanStockPrice`（重跑會翻倍），把兩張表擺一起比，你會非常有感。

---

## 檢查你是不是真的做到了

| # | 你應該看到 | 它證明了什麼 |
|---|-----------|-------------|
| 1 | 出現 `TaiwanStockPrice_duplicate` 表 | 複合主鍵的表建立成功 |
| 2 | 重跑後筆數不變 | upsert 讓操作冪等 |
| 3 | 對照 `TaiwanStockPrice` 會翻倍 | 有沒有主鍵 + upsert 差很多 |

---

## 想再深入一點

- **為什麼這一章要自己用 `Table(...)` 定義結構，第 5 章卻不用？** 因為 `on_duplicate_key_update`（upsert）要能運作，資料庫必須先知道「哪些欄位是主鍵」——它是靠「主鍵撞到了沒」來判斷該 insert 還是 update。第 5 章的 `to_sql` 自動建表不會幫你設主鍵，所以這一章改成明確用 `Table(...)` 宣告 `stock_id` + `date` 為複合主鍵，upsert 才有依據。
- **`insert_stmt.inserted[col.name]` 是什麼？** 這是 MySQL upsert 的慣用寫法，`inserted[...]` 代表「這次原本要插入的那個新值」。所以 `on_duplicate_key_update` 那段的意思就是「如果撞到主鍵，把每個欄位都更新成這次帶進來的新值」——等於用新資料覆蓋舊資料。
- **為什麼用 `iterrows()` 一列一列寫？慢不慢？** 慢。`iterrows()` 逐列跑迴圈、一列送一次 SQL，資料量大時效率不好。教學這樣寫是為了讓你看清楚「每一列都在做 upsert」的邏輯；正式環境會改成「批次 upsert」（一次送多列）來加速。你之後優化時可以往這個方向想。
- **換成別的資料庫呢？** `on_duplicate_key_update` 是 **MySQL 專屬**語法（所以 import 的是 `sqlalchemy.dialects.mysql.insert`）。如果換成 PostgreSQL，要改用 `ON CONFLICT (...) DO UPDATE`；概念一樣（upsert），但語法不同。這也是為什麼 import 要特別指定 MySQL 方言。
- **主鍵怎麼選是門學問。** 這裡選 `stock_id + date` 是因為它能唯一辨識「某股某日」這筆商業資料。換一種資料就要重想：例如「使用者的每日簽到」可能是 `user_id + date`、「訂單明細」可能是 `order_id + product_id`。選錯主鍵，冪等就會失效（要嘛擋不掉重複，要嘛蓋掉不該蓋的資料）。

---

## 想一想（確認你懂了）

先自己想過再看答案。

**Q1：用一句話解釋「冪等」，並舉一個生活例子。**

冪等 = 同一個操作做一次跟做很多次，結果一樣。生活例子：電梯樓層鍵（連按不會多帶你去幾層）、或「把開關撥到 ON」（撥幾次都還是 ON）。相對地，「再倒一杯水」就不冪等（做幾次就多幾杯）。

**Q2：為什麼 `stock_id` 單獨當主鍵不夠？為什麼要加 `date`？**

因為同一支股票有很多天的資料，如果只用 `stock_id` 當主鍵，2330 就只能存一筆，第二天的資料會把第一天蓋掉。要能「一支股票、多天各一筆」，唯一身分必須是「股票 + 日期」的組合，所以要用 `stock_id + date` 複合主鍵。

**Q3：如果 FinMind 事後修正了某天的收盤價，重跑這支任務會發生什麼？**

因為主鍵（該股該日）已存在，upsert 會走「更新」那條路，把那一筆的欄位**更新成新的收盤價**，而不是新增一筆。所以你的資料會被自動修正成最新值，且不會產生重複。這正是冪等在真實維運裡的價值。

---

## 換你試試看

**練習 1：親手對照「冪等」vs「不冪等」**

先跑第 5 章的 `producer_crawler_finmind.py` 三次，看 `TaiwanStockPrice` 筆數一直漲；再跑這章的 `producer_crawler_finmind_duplicate.py` 三次，看 `TaiwanStockPrice_duplicate` 筆數不動。把兩組數字並排寫下來，你就把「冪等」這個抽象詞變成看得到的東西了。

**練習 2：驗證「重複真的被去掉了」**

對去重表跑第 5 章練習 2 的那條 SQL：

```sql
SELECT stock_id, date, COUNT(*)
FROM TaiwanStockPrice_duplicate
GROUP BY stock_id, date
HAVING COUNT(*) > 1;
```

你應該得到**空結果**（沒有任何一組重複）。對照第 5 章的表會查到一堆重複。這讓你確認複合主鍵 + upsert 真的生效。

**練習 3：手動改一筆再重跑，觀察 update**

在 phpMyAdmin 手動把去重表裡某支股票某天的 `close` 改成一個明顯錯的值（例如 999999），然後重跑 producer。回去看那一筆——它會被改回 FinMind 的正確值。這讓你親眼看到 upsert 的「update」那一半在運作：重複的主鍵不是被跳過，是被更新。

---

## 卡住了？常見錯誤這樣排

| 你遇到的狀況 | 原因 | 怎麼解 |
|-------------|------|--------|
| `on_duplicate_key_update` 語法錯 | 這是 MySQL 專用語法 | 確認 import 的是 `sqlalchemy.dialects.mysql.insert`；換 PostgreSQL 要改用 `ON CONFLICT` |
| 重跑筆數還是變多 | 主鍵沒設對，或寫到了沒有主鍵的表 | 確認寫入的是 `TaiwanStockPrice_duplicate`、且 stock_id + date 都是主鍵 |
| 逐列 upsert 覺得慢 | `iterrows()` 一列一列寫 | 教學夠用；正式環境可改批次 upsert |

---

## 這一章你學到了

- 冪等 = 同操作做幾次結果都一致，是定期資料流的必備性質。
- 複合主鍵定義「一筆資料的唯一身分」，upsert 讓重跑安全。
- MySQL 用 `on_duplicate_key_update` 實作 upsert。

## 下一章要做什麼

到這裡，抓取、可靠性、落地、冪等都齊了。**下一章把你一路用到的三個 Web 管理介面（RabbitMQ、Flower、phpMyAdmin）做個總整理，建立一套「出事了先看哪裡」的排錯 SOP，為 Celery 階段收尾。**
